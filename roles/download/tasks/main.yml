---
- name: Check k3s x64
  shell: /usr/local/bin/k3s -v |head -1|cut -d' ' -f3
  register: check_k3s

- name: Download k3s binary x64
  get_url:
    #url: http://192.168.56.1:8000/k3s-v1.21.5/k3s
    url: http://192.168.56.1:8000/k3s-v1.22.2/k3s
    #url: http://github.com/k3s-io/k3s/releases/download/{{ k3s_version }}/k3s
    #checksum: sha256:http://github.com/k3s-io/k3s/releases/download/{{ k3s_version }}/sha256sum-amd64.txt
    dest: /usr/local/bin/k3s
    owner: root
    group: root
    mode: "u=rwx,g=x,o=x"
    timeout: 180
  when:
    - ansible_facts.architecture == "x86_64"
    - (check_k3s.changed) and (check_k3s.stdout != k3s_version)

- name: Download k3s binary arm64
  get_url:
    url: https://github.com/k3s-io/k3s/releases/download/{{ k3s_version }}/k3s-arm64
    checksum: sha256:https://github.com/k3s-io/k3s/releases/download/{{ k3s_version }}/sha256sum-arm64.txt
    dest: /usr/local/bin/k3s
    owner: root
    group: root
    mode: 0755
  when:
    - ( ansible_facts.architecture is search("arm") and
        ansible_facts.userspace_bits == "64" ) or
      ansible_facts.architecture is search("aarch64")

- name: Download k3s binary armhf
  get_url:
    url: https://github.com/k3s-io/k3s/releases/download/{{ k3s_version }}/k3s-armhf
    checksum: sha256:https://github.com/k3s-io/k3s/releases/download/{{ k3s_version }}/sha256sum-arm.txt
    dest: /usr/local/bin/k3s
    owner: root
    group: root
    mode: 0755
  when:
    - ansible_facts.architecture is search("arm")
    - ansible_facts.userspace_bits == "32"
